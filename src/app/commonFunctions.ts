/**
 * @param input value to be verified.. might be $event(to be handled later)
 *
 * @return true if ALL input chars are of number, else otherwise.
 */
export function verifyOnlyNums(input: string): boolean {
  if (input) {
    if (input.length) {
    }
  }

  return false;
}
